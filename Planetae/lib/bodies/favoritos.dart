import 'package:flutter/material.dart';

class bodyFavoritos extends StatelessWidget {
  const bodyFavoritos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("favoritos"),
    );
  }
}
